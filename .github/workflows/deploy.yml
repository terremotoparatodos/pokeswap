name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# A push to main deploys to the same Pages production as the playtest build:
# share its group so the two can never overlap. PR previews stay independent.
concurrency:
  group: ${{ github.event_name == 'push' && 'pages-production' || format('pages-preview-{0}', github.ref) }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Cloudflare Pages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      deployments: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ vars.VITE_SUPABASE_ANON_KEY }}
          VITE_REALTIME_URL: ${{ vars.VITE_REALTIME_URL }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=pokeswap --commit-dirty=true
          # On PRs, wrangler deploys a preview URL and posts it as a comment.
          # On main, it deploys to production.
